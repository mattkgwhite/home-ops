# Scripts

Bootstrap and helper shell scripts live here. Read each script before execution,
confirm its required environment variables, and never commit generated secrets.

TODO: Document supported platforms, idempotency, inputs, outputs, and dry-run
behavior for every script.
