# OpenTofu

No supported OpenTofu root module exists yet.

The Crossplane manifest includes an OpenTofu provider experiment, but that does
not establish this directory as a deployment root. Decide ownership and state
policy before implementation.
