# Infrastructure Inventory

This directory is reserved for a sanitised cross-platform inventory. Executable
Ansible inventory remains in [`ansible/inventory/`](../ansible/inventory/).

TODO: Record physical assets, logical hosts, roles, lifecycle status, and
documentation links without exposing secrets or sensitive recovery data.
