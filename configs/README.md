# Shared Configuration

This directory is reserved for non-secret configuration shared by multiple
platforms. Keep platform-owned configuration beside its implementation.

Do not store credentials, decrypted secrets, private keys, or kubeconfigs here.
