# Docker

This directory is reserved for supported Dockerfiles and Compose projects.
Archived Ansible Docker roles remain in place for history.

TODO: Add a service index, deployment notes, upgrade procedure, persistence
model, and backup links when a Docker workload is admitted.
